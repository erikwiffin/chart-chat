mutation SendMessage($projectId: ID!, $content: String!, $activeChartId: ID) {
  sendMessage(projectId: $projectId, content: $content, activeChartId: $activeChartId) {
    id
    content
    role
    createdAt
  }
}
